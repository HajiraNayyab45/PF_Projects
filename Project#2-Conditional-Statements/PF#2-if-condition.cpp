#include <iostream>
using namespace std;

int main() {
    int num;

    // Ask the user to enter a number
    cout << "Enter a number: ";
    cin >> num;

    // If condition to check if the number is positive
    if (num > 0) {
        cout << "The number is positive." << endl;
    }

    return 0;
}
